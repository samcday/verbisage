# D-Bus interface introspection XML
dbus_interfaces_dir = join_paths(
  get_option('datadir'), 'dbus-1', 'interfaces',
)

install_data(
  'org.verbisage.Dictionary.xml',
  install_dir: dbus_interfaces_dir,
)
